#include <iostream>
#include <vector>
#include <unordered_map>
#include <algorithm>
#include <string>

return std::reduce(derived.begin(), derived.end()) % 2 == 0;
return std::reduce(derived.begin(), derived.end(), 0, bit_xor<>()) == 0;

int main()
{
	std::vector<int> v = {};
	int ans = f(v);
	std::cout << ans << std::endl;
	return 0;
}
// runtime beats 100%
// memory beats 48.13%
